language: java
before_install:
    - sudo apt-get update
    - sudo apt-get install ruby
install:
    - gem install compass
before_script:
    - cd paw-webapp

cache:
    directories:
        - $HOME/.m2